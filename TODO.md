# TODO

## 次回着手

- [ ] `App.tsx` の「牌編集・画像反映まわり」のローカル変換ロジックを `lib` 化
- [ ] `App.tsx` に残る対局進行ロジック（鳴き適用/捨て牌遷移/局進行）の追加分離
- [ ] CSSトークン化の残りを完了（同系色の重複定義を整理）
- [ ] ログ系の `appendActionLog` 利用を統一（直接 `setActionLog` 呼び出しの残存確認）
